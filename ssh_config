# Shared defaults — host-specific entries go in ~/.ssh/config.local
Include ~/.ssh/config.local

Host *
    AddKeysToAgent yes
    IdentityFile ~/.ssh/id_ed25519
    ServerAliveInterval 60
    ServerAliveCountMax 3
    Compression yes

version: 2
updates:
  # The only dependency manifest in this repo is the GitHub Actions workflows.
  # Without this file nothing opens PRs, so dependabot-auto-merge.yml never runs.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
